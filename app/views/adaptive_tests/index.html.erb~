<div class="block">
  <% if current_user.role == 'admin' %>
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "List", adaptive_tests_path  %></li>
      <li><%= link_to "New", new_adaptive_test_path %></li>
    </ul>
  </div>
  <% end %>
  <div class="content">
    <h2 class="title"%> AdaptiveTests </h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attribu tes.adaptive_test.title", :default => t("activerecord.labels.title", :default => "Title")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        
        <% paginate_tests = @adaptive_tests.paginate(:per_page => 10, :page => params[:page]) %>
        <% paginate_tests.each do |test|%>
          <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= test.id %>
          </td>
                    <td>
            <%= link_to test.title, adaptive_test_path(test) %>
          </td>
                    <td>
            <%= test.created_at %>
          </td>
          <td class="last">
            <%= link_to "Run test", "/adaptive_tests/#{test.id}/run" %> |
            <% if can? :edit, AdaptiveTest %>
              <%= link_to "Edit", edit_adaptive_test_path(test) %> |
              <%= link_to "Delete", adaptive_test_path(test), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
            <% end %>
          </td>
        </tr>
        <% end %>
        
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
         <%= will_paginate paginate_tests %>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
